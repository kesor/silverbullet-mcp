"""mcp-silverbullet — Model Context Protocol bridge for SilverBullet.

See docs/design.md for the architecture and docs/wayfinder/map.md for
the build map. The current ticket (T1) only ships the package shell;
the MCP server, SB client, and verifier land in T2–T4.
"""

__version__ = "0.1.0"