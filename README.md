# mcp-silverbullet

> **Stub.** The real README (boot order, env vars, tunnel notes) lands in
> T6 of the build map — see `docs/wayfinder/map.md`. Kept here so
> hatchling can read it during `uv sync`.

A Model Context Protocol bridge between SilverBullet and MCP clients
(e.g. Grok on the web). Architecture in [`docs/design.md`](docs/design.md);
build map in [`docs/wayfinder/map.md`](docs/wayfinder/map.md).